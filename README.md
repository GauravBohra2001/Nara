# NARA

Women's health intelligence app with a FastAPI backend, cycle phase modeling, Apple Health XML parsing, and a Next.js frontend.

## Run

Backend:

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --port 8000 --reload
```

Frontend:

```bash
cd frontend
npm install
npm run dev
```

Open `http://localhost:3000`.
